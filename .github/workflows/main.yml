name: Build Pro App (Android & Windows)
on: [push]

jobs:
  # אנדרואיד נשאר ללא שינוי כי הוא עובד מעולה
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Unzip and Move Files
        run: |
          unzip project.zip -d temp_folder
          ROOT_DIR=$(find temp_folder -name "assets" -exec dirname {} \;)
          cp -R $ROOT_DIR/* .
          find temp_folder -name "icon.png" -exec cp {} . \;
          rm -rf temp_folder
      - name: Create Main.dart
        run: |
          mkdir -p lib
          cat <<EOF > lib/main.dart
          import 'dart:io' show Platform;
          import 'package:flutter/material.dart';
          import 'package:webview_flutter/webview_flutter.dart';
          import 'package:permission_handler/permission_handler.dart';
          void main() async {
            WidgetsFlutterBinding.ensureInitialized();
            runApp(const MaterialApp(debugShowCheckedModeBanner: false, home: WebViewPage()));
          }
          class WebViewPage extends StatefulWidget {
            const WebViewPage({super.key});
            @override State<WebViewPage> createState() => _WebViewPageState();
          }
          class _WebViewPageState extends State<WebViewPage> {
            late final WebViewController controller;
            @override
            void initState() {
              super.initState();
              controller = WebViewController()
                ..setJavaScriptMode(JavaScriptMode.unrestricted)
                ..setBackgroundColor(const Color(0x00000000))
                ..loadFlutterAsset('assets/index.html');
            }
            @override
            Widget build(BuildContext context) {
              return Scaffold(body: SafeArea(child: WebViewWidget(controller: controller)));
            }
          }
          EOF
      - name: Fix Pubspec
        run: |
          cat <<EOF > pubspec.yaml
          name: kids_browser
          version: 1.0.0+1
          environment:
            sdk: ">=3.0.0 <4.0.0"
          dependencies:
            flutter:
              sdk: flutter
            webview_flutter: ^4.10.0
            webview_flutter_android: ^4.12.0
            permission_handler: ^11.3.1
          dev_dependencies:
            flutter_launcher_icons: ^0.13.1
          flutter_icons:
            android: true
            image_path: "icon.png"
          flutter:
            uses-material-design: true
            assets:
              - assets/
          EOF
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true
      - name: Recreate Android Project
        run: |
          rm -rf android
          flutter create . --platforms android --org com.kids.browser
      - name: Generate Icons
        run: flutter pub run flutter_launcher_icons || true
      - name: Add Permissions
        run: |
          sed -i '/<application/i \    <uses-permission android:name="android.permission.INTERNET"/>\n    <uses-permission android:name="android.permission.RECORD_AUDIO" />\n    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />\n    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />\n    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />' android/app/src/main/AndroidManifest.xml
          sed -i 's/<application/<application android:usesCleartextTraffic="true" android:hardwareAccelerated="true"/' android/app/src/main/AndroidManifest.xml
      - name: Build APK
        run: flutter build apk --release --no-tree-shake-icons --split-per-abi
      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: android-release
          path: build/app/outputs/flutter-apk/*.apk

  # ווינדוס - גרסה מתוקנת לבעיות קידוד
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Unzip Project
        shell: pwsh
        run: Expand-Archive -Path project.zip -DestinationPath temp_zip -Force

      - name: Extract Assets Only
        shell: pwsh
        run: |
          $assetsPath = Get-ChildItem -Path temp_zip -Recurse -Directory -Filter "assets" | Select-Object -First 1
          if ($assetsPath) {
              Copy-Item -Path "$($assetsPath.FullName)" -Destination "." -Recurse -Force
          }
          # יצירת תיקיית lib ריקה כדי שפלאטר לא יתלונן
          if (!(Test-Path lib)) { New-Item -ItemType Directory -Path lib }

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Create Clean Project and Build
        shell: pwsh
        run: |
          # יצירת קובץ pubspec נקי לחלוטין בפורמט UTF8 בתוך השרת
          $pubContent = @"
          name: kids_browser
          version: 1.0.0+1
          environment:
            sdk: ">=3.0.0 <4.0.0"
          dependencies:
            flutter:
              sdk: flutter
            webview_windows: ^0.4.0
          flutter:
            uses-material-design: true
            assets:
              - assets/
          "@
          $pubContent | Out-File -FilePath pubspec.yaml -Encoding utf8
          
          # יצירת קובץ main.dart בסיסי לווינדוס
          $mainContent = @"
          import 'package:flutter/material.dart';
          import 'package:webview_windows/webview_windows.dart';
          void main() => runApp(MaterialApp(home: Scaffold(body: WebviewPage())));
          class WebviewPage extends StatefulWidget { @override _WebviewPageState createState() => _WebviewPageState(); }
          class _WebviewPageState extends State<WebviewPage> {
            final _controller = WebviewController();
            @override
            void initState() { super.initState(); initPlatformState(); }
            Future<void> initPlatformState() async {
              await _controller.initialize();
              await _controller.loadUrl('https://google.com'); // נשנה את זה בהמשך לנתיב מקומי
              if (!mounted) return;
              setState(() {});
            }
            @override
            Widget build(BuildContext context) => WebviewWidget(_controller);
          }
          "@
          $mainContent | Out-File -FilePath lib/main.dart -Encoding utf8

          flutter create . --platforms windows
          flutter build windows --release

      - name: Upload Windows App
        uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: build/windows/x64/runner/Release/
